import {AppMergerDirective} from './app-merger.directive';

describe('AppMergerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMergerDirective();
    expect(directive).toBeTruthy();
  });
});
